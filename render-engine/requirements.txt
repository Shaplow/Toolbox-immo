# gradio est intentionnellement absent ici : il est uniquement utilisé par app.py (UI locale hors Docker).
# L'inclure crée un conflit huggingface-hub entre gradio 6.x et pyannote-audio 4.x sur aarch64,
# ce qui force pip à backtracker jusqu'à pyannote 3.x → lightning>=2.0.1 introuvable sur aarch64.
# Pour utiliser app.py en dehors de Docker : pip install gradio
pillow>=10.4.0
pydantic>=2.8.0
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
aiofiles>=24.1.0
httpx>=0.27.0
boto3>=1.34.0

# Transcription (WhisperX + faster-whisper)
# Épinglé à 3.8.5 : la contrainte >=3.1.0 déclenchait un backtracking massif de pip
# (3.8.x → 3.7.x → ... → 3.2.0) qui finissait sur pyannote.audio==3.1.1 → lightning fail.
whisperx==3.8.5
faster-whisper>=1.0.0

# Derush vision pipeline
scenedetect[opencv-headless]>=0.6.4
opencv-python-headless>=4.10.0
numpy>=1.26.0
imagehash>=4.3.1