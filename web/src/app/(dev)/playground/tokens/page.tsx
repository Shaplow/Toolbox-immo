/**
 * Placeholder — sera rempli en Phase 1 (tokens design system).
 * Visualise palette, typo, espacements, ombres et animations.
 */
export default function TokensPage() {
  return (
    <div className="space-y-6">
      <header className="space-y-1">
        <h1 className="text-2xl font-semibold tracking-tight">Tokens</h1>
        <p className="text-sm text-gray-500">
          Palette, typographie, espacements, ombres, animations. Source de
          vérité du design system.
        </p>
      </header>
      <div className="rounded-lg border border-dashed border-gray-300 bg-gray-50 p-10 text-center text-sm text-gray-400">
        À remplir en Phase 1.
      </div>
    </div>
  );
}
