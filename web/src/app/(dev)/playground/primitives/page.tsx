/**
 * Placeholder — sera rempli en Phase 1 (primitives design system).
 * Visualise Button, Input, FormField, Dialog, Tooltip, etc. avec tous
 * leurs variants/états (hover, focus, disabled, loading, error).
 */
export default function PrimitivesPage() {
  return (
    <div className="space-y-6">
      <header className="space-y-1">
        <h1 className="text-2xl font-semibold tracking-tight">Primitives</h1>
        <p className="text-sm text-gray-500">
          Composants atomiques (Button, Input, Dialog, Tooltip, etc.) avec
          tous leurs variants et états.
        </p>
      </header>
      <div className="rounded-lg border border-dashed border-gray-300 bg-gray-50 p-10 text-center text-sm text-gray-400">
        À remplir en Phase 1.
      </div>
    </div>
  );
}
